<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:android="clr-namespace:Xamarin.Forms.PlatformConfiguration.AndroidSpecific;assembly=Xamarin.Forms.Core" 
             xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" 
             xmlns:macOS="clr-namespace:Xamarin.Forms.PlatformConfiguration.macOSSpecific;assembly=Xamarin.Forms.Core" 
             xmlns:windows="clr-namespace:Xamarin.Forms.PlatformConfiguration.WindowsSpecific;assembly=Xamarin.Forms.Core" 
             xmlns:behaviors="clr-namespace:Prism.Behaviors;assembly=Prism.Forms" 
             xmlns:ioc="clr-namespace:Prism.Ioc;assembly=Prism.Forms" 
             xmlns:i18n="using:GroceryHelper.i18n" 
             Title="{Binding Title}" 
             x:Class="GroceryHelper.Views.EditItemPage">

    <StackLayout Padding="20" BackgroundColor="White">
        <Label Text="{i18n:Translate DescriptionLabel}" />

        <Entry Text="{Binding EditItem.Name}" Placeholder="Name" />
        <Entry Text="{Binding EditItem.Aisle}" Placeholder="Aisle" />
        <Entry Text="{Binding EditItem.Type}" Placeholder="Type" />
        <Entry Text="{Binding EditItem.Notes}" Placeholder="Notes" />
 
        <Button Text="{i18n:Translate SaveButton}" Command="{Binding SaveCommand}" />
    </StackLayout>
</ContentPage>
